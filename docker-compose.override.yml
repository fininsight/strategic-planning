services:
  frontend:
    ports:
      - "8080:80"

  api:
    volumes:
      - ./.local-data/attachments:/data/attachments
      - ./.local-data/analyses:/data/analyses
      - ./.local-data/company-knowledge:/data/company-knowledge
      - ./.local-data/logs:/app/logs
